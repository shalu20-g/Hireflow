# Copy to .env and fill in local values. Never commit .env.
PORT=5000
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/hireflow
JWT_SECRET=change-me-to-a-long-random-string
# Local frontend origin allowed by CORS (defaults to http://localhost:5173 if unset).
FRONTEND_URL=http://localhost:5173
